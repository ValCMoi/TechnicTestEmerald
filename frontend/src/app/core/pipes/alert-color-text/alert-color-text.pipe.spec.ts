import { AlertColorTextPipe } from './alert-color-text.pipe';

describe('AlertColorTextPipe', () => {
  it('create an instance', () => {
    const pipe = new AlertColorTextPipe();
    expect(pipe).toBeTruthy();
  });
});
